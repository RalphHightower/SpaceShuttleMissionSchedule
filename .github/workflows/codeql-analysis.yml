on:
  push:
    branches: [main, protected]
  pull_request:
    branches: [main]
    paths-ignore:
      - '**/*.md'
      - '**/*.txt'
jobs:
  analyze:
    name: Analyze
    runs-on: [windows-latest]
